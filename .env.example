# Kisan Setu - Environment Variables
PORT=5001
NODE_ENV=development

# Database (Optional - defaults to zero-config persistent JSON file engine)
MONGODB_URI=mongodb://localhost:27017/kisan-setu

# JWT Authentication Secret (For production migration)
JWT_SECRET=kisan_setu_secret_key_sih2026

# Client URL (For CORS whitelist in production)
CLIENT_URL=http://localhost:3000
